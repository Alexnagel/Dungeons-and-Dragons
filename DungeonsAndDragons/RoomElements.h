#pragma once
#include <vector>
#include <string>
#include <random>

class RoomElements
{
	/*
private:
	int seed;
	std::mt19937 rng;
	std::vector<std::string> sizeVector;
	std::vector<std::string> stateVector;
	std::vector<std::string> lightingVector;
	std::vector<std::string> itemVector;
	std::vector<std::string> floorVector;
	std::vector<std::string> placeVector;

	int GetRandomNumber(int max);
	void fillVectors();
public:
	RoomElements();
	virtual ~RoomElements();

	std::string GetRandomSize();
	std::string GetRandomState();
	std::string GetRandomLighting();
	std::string GetRandomItem();
	std::string GetRandomFloorType();
	std::string GetRandomPlace();
	*/
};

